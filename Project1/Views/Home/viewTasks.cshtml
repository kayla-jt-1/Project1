@{
    ViewData["Title"] = "Tasks";
}

@model List<TaskModel>

@model Project1.Models.TaskModel


<div class="container">
    <div class="row">
        <div class="col-2"></div>
        <div class="col-5">
            <p class="font-weight-bold">Urgent</p>

            @if (Model.Completed == true)
            {
                @Model.TaskName
            }

            <a asp-controller="Home" asp-action="Edit" asp-route-id="@x.TaskId">Update</a></td>
            <a asp-controller="Home" asp-action="Delete" asp-route-id="@x.TaskId">Delete</a></td>

        </div>
        <div class="col-5">
            <p class="font-weight-bold">Not Urgent</p>

            @if (Model.Completed == true)
            {
                @Model.TaskName
            }

            <a asp-controller="Home" asp-action="Edit" asp-route-id="@x.TaskId">Update</a></td>
            <a asp-controller="Home" asp-action="Delete" asp-route-id="@x.TaskId">Delete</a></td>

        </div>
    </div>

    <div class="row">
        <div class="col-2">
            <p class="font-weight-bold">Important</p>

            @if (Model.Completed == true)
            {
                @Model.TaskName
            }

            <a asp-controller="Home" asp-action="Edit" asp-route-id="@x.TaskId">Update</a></td>
            <a asp-controller="Home" asp-action="Delete" asp-route-id="@x.TaskId">Delete</a></td>

        </div>
        <div class="col-5"></div>
        <div class="col-5"></div>
    </div>

    <div class="row">
        <div class="col-2">
            <p class="font-weight-bold">Not Important</p>

            @if (Model.Completed == true)
            {
                @Model.TaskName
            }

            <a asp-controller="Home" asp-action="Edit" asp-route-id="@x.TaskId">Update</a></td>
            <a asp-controller="Home" asp-action="Delete" asp-route-id="@x.TaskId">Delete</a></td>

        </div>
        <div class="col-5"></div>
        <div class="col-5"></div>
    </div>
</div>

   


